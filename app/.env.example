# Dev: leave unset to use Vite proxy → http://localhost:5001
# Production: set full API URL, e.g. https://api.example.com/api
# VITE_API_URL=/api

# Optional: Socket.IO origin (defaults to window origin in dev, or derived from VITE_API_URL host)
# VITE_SOCKET_URL=https://api.example.com
